import Layout from "@theme/Layout";

export default function SubmitPage() {
  return (
    <Layout title="Submit">
      <main className="container margin-vert--lg">
        <h1>Submit Materials</h1>
        <p>这一页会承接站内可视化投稿流程。当前先保留为可访问入口，下一步补完整表单与 GitHub Issue 预览。</p>
      </main>
    </Layout>
  );
}
